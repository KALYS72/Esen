#include <iostream>
#include <cmath>
#include <random>
using namespace std;
int main() {
    /*double n = -1, k=0, I = 0;
    while(n != 1) {
        double r_f = n / 1, r_f_r = r_f * n;
        cout << "r: " << n << " f(r): " << r_f << " r(f) * r: " << r_f_r << endl;
        I+=n;
        n+=0.00001;
        k++;
    }
    cout << "I = " << (I/k);
    double a, b, n, I = 0, k = 0;
    cout << "a: ";
    cin >> a;
    cout << "\nb: ";
    cin >> b;
    cout << "\nn: ";
    cin >> n;
    while(n >= k) {
        double r = (b-a) * (rand() % 2) + a;
        I = I + sin(r) * r;
        k++; 
    }
    cout << (I/k);
    */
}