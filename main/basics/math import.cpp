#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int a;
    int b;
    cin >> a;
    cin >> b;
    int res_a = pow(a,2); // степень. да
    int res_b = pow(b,2);
    cout << (res_a + res_b);
}